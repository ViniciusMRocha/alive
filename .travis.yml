language: node_js
node_js:
  - "lts/*"

branches:
  only:
    - master

cache:
  yarn: true

before_script:
  - npm install firebase-tools -g
  - cd functions && npm install

script:
  - yarn test --coverage
  - yarn build
  - firebase deploy
